[build]
builder = "NIXPACKS"
buildCommand = "python -m pip install -r requirements-simplemem.txt && npm run build"

[deploy]
startCommand = "npm start"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
