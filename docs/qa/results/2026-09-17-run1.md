# QA Run — 2026-09-17 (Run 1)

- **Executor:** Buffy (QA Executor AI) via playwriter.dev driving user's Chrome
- **Build/commit:** `478b56d` (branch `main`)
- **Pre-flight gate:** `npm test` 288 suites / 1511 tests PASS (11 skipped live) · `tsc --noEmit` PASS · `lint` PASS (0 errors, 72 pre-existing warnings) · `check:design` PASS (3 warnings) → **execution permitted**
- **Environment notes:**
  - Expo dev server booted fresh on `:8081` (web), Metro bundled ~90s.
  - Browser: user's Chrome via playwriter.dev (extension relay), session 1.
  - **Supabase auth gateway unreachable all session** (`EXPO_PUBLIC_SUPABASE_URL=http://100.107.7.52:54321`; page-context fetch → `TypeError: Failed to fetch`; console `AuthRetryableFetchError`). App booted on cached AsyncStorage session (`sigmundsarino@gmail.com`) with ~10s restore latency.
  - Theme: app native Dark at start; switched to Light mid-run (verified live-apply, surface `rgb(244,241,235)`).
  - Storage landscape: 3 account scopes in localStorage (05e18a1e…, db07ce4d…, active 16cdd861…).

## Cases executed

| Case ID | Title | Status | Notes |
|---|---|---|---|
| AUTH-01 | Login valid | Blocked | Supabase unreachable (page-context probe) |
| AUTH-02 | Login invalid password | Blocked | same |
| AUTH-03 | Login empty fields | Blocked | login screen unreachable behind cached session |
| AUTH-04 | Signup new | Blocked | same as AUTH-01 |
| AUTH-05 | Signup duplicate | Blocked | same |
| AUTH-06 | Forgot password | Blocked | same |
| AUTH-07 | Update password | Blocked | same + no reset link |
| AUTH-08 | Logout | Blocked | **deliberate hold**: executing logout while Supabase is down would destroy the only cached session and strand the run behind a login wall. Re-run FIRST when Supabase is back |
| AUTH-09 | Session persists across reload | **Pass** | reload on /settings: stayed authenticated, no login prompt, no logged-out flash (restore ~10s) |
| AUTH-10 | Auth screens both schemes | Blocked | auth routes redirect while signed in; signed-out state unavailable (see AUTH-08 hold) |
| GOALS-01 | Create goal | **Pass** | created via dialog; persisted across reload (storage ground truth) |
| GOALS-02 | Edit goal | **Fail** | no edit affordance exists (UI gap) → DEF-001 |
| GOALS-03 | Complete/uncomplete | **Pass** | storage flip verified across 3 clicks; found DEF-002 (aria-checked null) as byproduct |
| GOALS-04 | Delete goal | **Fail** | no delete affordance exists (UI gap) → DEF-001 |
| GOALS-05 | Spacing vs concept (P1 regression) | **Pass** | code: gap-3/gap-3.5/min-h-14/border-t, zero space-*; DOM row measurements + screenshot `output/qa/GOALS-05-light-spacing.png`; concept pixel-diff blocked by env — human eyeball still recommended |
| GOALS-06 | Empty goals state | **Pass** | both strings + CTA visible light (≈4.8:1) + dark observed pre-seed; screenshot `output/qa/GOALS-06-light-empty.png` |
| GOALS-07 | Many goals scroll | Blocked | needs 10+ visible goals — blocked by DEF-003 |
| SETTINGS-05 | Seed demo data | **Fail** | entries seed OK (13), but check-ins=0 + goals=0 on active scope while record lists 6+12 ids existing in no scope → DEF-003 |

## Defects opened

- **DEF-001** (S3, GOALS-02/04): goals screen has no edit/delete UI; service layer supports both unused.
- **DEF-002** (S4, GOALS-03): checkbox `aria-checked` null on web — screen-reader users get no completion state.
- **DEF-003** (S2, SETTINGS-05): demo seed silently fails to persist check-ins + goals for active account scope; record tracks ids that exist nowhere; dialog promises goals. Suspect account-scope write flapping (run-1 goals landed in foreign scope). **Demo clear (SETTINGS-06) will no-op on missing ids — re-test both after fix.**
- **DEF-004** (S2, GOALS-01 variant / LIFE-06 territory): alien-shape goals key (`[]` array) silently freezes ALL goal writes — dialog closes, zero storage writes, no error, no self-heal; writes resume after restoring `{}` map. Corrupt-shape tolerance gap (AGENTS.md rule 4 pattern). Reproduced 3× incl. fresh boot.

## Executor incidents (full transparency)

1. Mid-run, executor overwrote the active account's goals key with `[]` intending to remove one test goal — wiped 13 seeded goals (no backup taken). This was executor error, not app behavior. Recovery: re-seed via app (SETTINGS-05 run 1). The incident *did* surface DEF-004 (writes silently frozen on alien shape) — the isolation protocol (array → `{}` → writes resume) is the only reason DEF-004's root cause is precise rather than anecdotal.
2. Run-1 seed evidence (goals=12 in foreign scope) is partially contaminated by incident 1; run-2 evidence (clean `{}` shape) is the authoritative DEF-003 basis.

## Carry-over / follow-ups

- Re-run AUTH-01…08 + AUTH-10 when Supabase gateway is reachable; run AUTH-08 (logout) **first**.
- GOALS-07 after DEF-003 fix (10+ goals in list).
- SETTINGS-06 (clear demo data) re-test after DEF-003 fix — expected to no-op on missing ids today.
- Human eyeball of GOALS-05 vs `black-rose-goals.png` concept (env blocked pixel compare).
- JOURNAL/CHECKINS/MEMORY/IDENTITY/THEME/etc. suites untouched this run (90 cases remain Untested).

**Scorecard: 18 dispositioned — 5 Pass · 3 Fail · 10 Blocked · 4 defects (1×S2 seed integrity, 1×S2 corrupt-shape data safety, 1×S3 UI gap, 1×S4 a11y)**
