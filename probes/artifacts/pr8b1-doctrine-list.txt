# PR8b-1 doctrine test list + diffs

## Enumerated load-bearing doctrine (from pre-diet code)

### (a) Date doctrine — buildClockContext (UNCHANGED, still injected every turn)
1. "## Date doctrine (write day vs event day)"
2. Written labels = write/finish day, NOT event day (full sentence about digests/capsule)
3. Weekday names in user prose authoritative for event timing
4. Never call event today/yesterday unless arithmetic holds
5. Event: YYYY-MM-DD label absolute when present

### (b) Identity trust — formatIdentityContext
OLD header:
## Identity (always-on core memory)
These facts are confirmed on-device about THIS user. Use them naturally (name in greeting, correct pronouns).
Do not invent identity details that are not listed. If a fact conflicts with the live message, trust the live message and treat the stored value as possibly outdated.

NEW header (load-bearing line KEPT verbatim):
## Identity
Confirmed on-device facts about THIS user. Use naturally (name, pronouns).
Do not invent identity details that are not listed. If a fact conflicts with the live message, trust the live message and treat the stored value as possibly outdated.

### (c) Safety/crisis — companion static (KEPT, compressed but pins present)
- hopelessness / self-harm ideation
- emergency resources / crisis service disclaimer
- never mock or dismiss crisis feelings

### (d) Memory conflict in companion static
"If memory conflicts with the live message, trust the live message and ask gently"

### Persona pins (new)
- Reflect first
- at most ONE gentle suggestion per turn
- Prefer a question over advice
- No unsolicited advice lists
