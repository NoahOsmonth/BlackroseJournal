const React = require('react');
const { Text } = require('react-native');

const Marked = ({ value }) => React.createElement(Text, null, value);

module.exports = {
  __esModule: true,
  default: Marked,
};
